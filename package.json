{
  "name": "rjp-stream",
  "version": "1.2.0",
  "private": true,
  "description": "RJP Stream V1.2 - agregador multimédia responsivo com fontes e atalhos Web pessoais",
  "scripts": {
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "preview": "npx serve dist -l 4173",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npm run build && npx cap sync android",
    "android:patch": "node scripts/patch-android.mjs",
    "build:tv": "node scripts/build-tv.mjs",
    "check": "npm run smoke && npm run build && node --check src/app.js"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
